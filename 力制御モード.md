提供された資料に基づき、モーターを力制御モード（MITモード）で動かす場合に使用する**5種類の設定値（パラメータ）**について解説します。

力制御モードでは、常に1つの通信IDを使用して以下の5つのパラメータを送信し、その値の組み合わせによって「位置制御」「速度制御」「トルク制御」を切り替えます。

### 1. 設定値の5つの種類と意味

*   **目標位置 (Position / `des P`)**
    *   **意味:** モーターを移動させたい目標の角度です。
    *   **単位:** **ラジアン (rad)** が使用されます（サーボモードで使用される「度（degree）」ではない点に注意が必要です）。
*   **目標速度 (Velocity, Speed / `des S`)**
    *   **意味:** モーターを回転させたい目標の速度です。
    *   **単位:** **ラジアン毎秒 (rad/s)** が使用されます。
*   **目標トルク (Torque, Current / `des T`)**
    *   **意味:** モーターから出力させたい目標のトルク値です。
    *   **単位:** **ニュートンメートル (N·m)** が使用されます。
*   **Kp (比例ゲイン / `KP`)**
    *   **意味:** 目標位置に留まろうとする「硬さ（バネ定数）」に相当するパラメータです。この値が大きいほど、外力に対して位置を固く保持しようとします。
*   **Kd (微分ゲイン / `KD`)**
    *   **意味:** 動きに対する「粘性・減衰（ダンパー）」に相当するパラメータです。動きを滑らかにしたり、振動を抑えたりする効果があります。

---

### 2. 制御モードごとの設定値の組み合わせ

どのような動作をさせたいか（位置、速度、トルク）によって、これら5つの設定値への値の入れ方が異なります。

#### ① 力制御 位置モード (Position Loop Mode)
指定した硬さ・粘性で特定の角度に移動させたい場合に使用します。
*   **設定する値:** **目標位置**、**Kp**、**Kd** の3つに具体的な数値を指定します。
*   （目標速度と目標トルクは基本的に0にします）

#### ② 力制御 速度モード (Velocity Loop Mode)
指定した粘性で特定の速度で回転させたい場合に使用します。
*   **設定する値:** **目標速度**、**Kd** の2つに具体的な数値を指定します。
*   **Kpは必ず `0` に設定します。** Kpを0にすることで位置の保持力がなくなり、速度指定での回転が可能になります。

#### ③ 力制御 トルクモード (Torque Loop Mode)
バネのような特性を持たせず、純粋に指定した一定の力を出し続けたい場合に使用します。
*   **設定する値:** **目標トルク** に具体的な数値を指定します。
*   **KpとKdは共に `0` に設定します。** これにより位置や速度に関わらず、指定トルクのみを出力する状態になります。

このように、力制御モードでは「Kp」や「Kd」をゼロにするかどうかで、モーターの動作モードをリアルタイムかつシームレスに切り替えることができるのが特徴です。
